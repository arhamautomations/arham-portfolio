# Arham Robotics Portfolio — React

Converted from the current HTML portfolio into a Vite + React project.

## Run
```bash
npm install
npm run dev
```

The Three.js robot arm is provided through the `three` npm package, and the existing interactive behavior is initialized from `src/portfolio.js`.
